import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';

//import Component from '../client/components/Component';

describe('<Component />', _ => {
  xit('does something', done => {
    done();
  });
});
